# Enable URL rewriting
RewriteEngine On

# If the requested file or directory exists, serve it
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Otherwise, append .html to the URL
RewriteRule ^([^/]+)/?$ $1.html [L]
